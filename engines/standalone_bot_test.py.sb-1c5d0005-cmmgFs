# ✅ standalone_bot_test.py – Simulates full bot launcher flow with test signals

import logging
import time
from queue import Queue
from og_bot_family_launcher import launch_bot_family
import upgrade_import_patch
from datetime import datetime

# Configure logging
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(message)s')

# ✅ Shared Queues
signal_queue = Queue()
bot_signal_queues = {
    "OGHeadTrader": Queue(),
    "OGPreoff": Queue(),
    "OGInPlay": Queue()
}

# ✅ Patch queues into upgrade import patch for downstream use
upgrade_import_patch.bot_signal_queues = bot_signal_queues

# ✅ Dummy managers to satisfy launcher injection
class DummyRiskManager:
    pass

class DummyBudgetManager:
    pass

# ✅ Test signal payloads (realistic structure pulled from database)
from daily_config import SESSION_TOKEN

now = datetime.utcnow().isoformat()

# Realistic test data (bot_name determines routing)
test_signals = [
    # HeadTrader tests (all types)
    {"status": "approved", "bet_type": "BASIC", "marketId": "1.TEST_HT", "selectionId": 101, "odds": 4.5, "stake": 10.0, "customerOrderRef": "HT_BASIC", "marketStartTime": now, "bot_name": "OGHeadTrader", "session_token": SESSION_TOKEN},
    {"status": "approved", "bet_type": "LADDER", "marketId": "1.TEST_HT", "selectionId": 102, "odds": 5.5, "stake": 10.0, "customerOrderRef": "HT_LADDER", "marketStartTime": now, "bot_name": "OGHeadTrader", "session_token": SESSION_TOKEN},
    {"status": "approved", "bet_type": "SCALP", "marketId": "1.TEST_HT", "selectionId": 103, "odds": 6.5, "stake": 10.0, "customerOrderRef": "HT_SCALP", "marketStartTime": now, "bot_name": "OGHeadTrader", "session_token": SESSION_TOKEN},
    {"status": "approved", "bet_type": "GREENUP", "marketId": "1.TEST_HT", "selectionId": 104, "odds": 3.5, "stake": 10.0, "customerOrderRef": "HT_GREENUP", "marketStartTime": now, "bot_name": "OGHeadTrader", "session_token": SESSION_TOKEN},
    {"status": "approved", "bet_type": "INPLAY", "marketId": "1.TEST_HT", "selectionId": 105, "odds": 7.0, "stake": 10.0, "customerOrderRef": "HT_INPLAY", "marketStartTime": now, "bot_name": "OGHeadTrader", "session_token": SESSION_TOKEN},

    # Preoff tests (no greenup or inplay)
    {"status": "approved", "bet_type": "BASIC", "marketId": "1.TEST_PO", "selectionId": 201, "odds": 4.5, "stake": 10.0, "customerOrderRef": "PO_BASIC", "marketStartTime": now, "bot_name": "OGPreoff", "session_token": SESSION_TOKEN},
    {"status": "approved", "bet_type": "LADDER", "marketId": "1.TEST_PO", "selectionId": 202, "odds": 5.5, "stake": 10.0, "customerOrderRef": "PO_LADDER", "marketStartTime": now, "bot_name": "OGPreoff", "session_token": SESSION_TOKEN},
    {"status": "approved", "bet_type": "SCALP", "marketId": "1.TEST_PO", "selectionId": 203, "odds": 6.5, "stake": 10.0, "customerOrderRef": "PO_SCALP", "marketStartTime": now, "bot_name": "OGPreoff", "session_token": SESSION_TOKEN},

    # InPlay tests (only inplay)
    {"status": "approved", "bet_type": "INPLAY", "marketId": "1.TEST_IP", "selectionId": 301, "odds": 3.3, "stake": 10.0, "customerOrderRef": "IP_INPLAY", "marketStartTime": now, "bot_name": "OGInPlay", "session_token": SESSION_TOKEN}
]

# ✅ Inject signals based on bot_name

# Insert deliberately broken signals first to simulate failure before valid ones
broken_signals = [
    {"status": "approved", "bet_type": "BASIC", "marketId": "1.BAD_HT_1", "selectionId": 9991, "stake": 10.0, "customerOrderRef": "BROKEN_MISSING_ODDS", "marketStartTime": now, "bot_name": "OGHeadTrader"},
    {"status": "approved", "bet_type": "LADDER", "marketId": "1.BAD_HT_2", "odds": 4.2, "customerOrderRef": "BROKEN_MISSING_SELECTION", "marketStartTime": now, "bot_name": "OGHeadTrader"},
    {"status": "approved", "bet_type": "SCALP", "selectionId": 9993, "odds": 4.2, "stake": 10.0, "customerOrderRef": "BROKEN_MISSING_MARKET", "marketStartTime": now, "bot_name": "OGHeadTrader"},
    {"status": "approved", "bet_type": "GREENUP", "marketId": "1.BAD_HT_4", "selectionId": 9994, "odds": 3.0, "customerOrderRef": "BROKEN_MISSING_STAKE", "bot_name": "OGHeadTrader"},
    {"status": "approved", "bet_type": "INPLAY", "marketId": "1.BAD_HT_5", "selectionId": 9995, "odds": 5.0, "stake": 10.0, "customerOrderRef": "BROKEN_NO_TIME", "bot_name": "OGHeadTrader"}
]

# Insert broken signals first
for signal in broken_signals:
    bot_signal_queues[signal["bot_name"]].put(signal)

# Insert valid signals after
for signal in test_signals:
    bot_signal_queues[signal["bot_name"]].put(signal)
launch_bot_family(
    headers={"X-Authentication": "TEST_TOKEN", "X-Application": "TEST_APP"},
    risk_mgr=DummyRiskManager(),
    bot_assignments={},
    bot_budget_limits={},
    toggles={},
    budget_mgr=DummyBudgetManager(),
    bot_signal_queues=bot_signal_queues
)

# Keep test running long enough to observe behavior
logging.info("🧪 [Standalone Test] Bots launched with test signals. Watching for output...")
time.sleep(10)
